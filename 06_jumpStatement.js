

//Break

for (let index = 0; index < 10; index++) {
    console.log(`Iterration Start`);
    
    if (index ==6) {
        break;
    }
    console.log(`Value Is-${index}  Iteration is ${index+1}`);
    console.log(`---End Iteration---`);

}







//COntinue 

for (let index = 0; index < 10; index++) {
    console.log(`Iterration Start`);
    
    if (index ==6) {
        continue;
    }
    console.log(`Value Is-${index}  Iteration is ${index+1}`);
    console.log(`---End Iteration---`);

}